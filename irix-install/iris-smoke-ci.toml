# Ephemeral CI smoke config (generated for Phase 3 verification).
headless = true
ci = true
ci_socket = "127.0.0.1:19851"
no_audio = false
banks = [128, 128, 0, 0]
nvram = "irix-install/nvram-irix65.bin"
scale = 1

[machine]
profile = "indy_ip24"

[perf]
thread_affinity = false

[audio]
prebuf_ms = 20

[scsi.1]
path = "irix-install/scsi1.raw"
cdrom = false

[nfs]
shared_dir = "shared"
